#include <bits/stdc++.h>
using namespace std ; 
 
int main (){
    ios::sync_with_stdio(0);
    cin.tie(0);
 
    int t ; // test cases
    cin >> t ; 
    while (t--){
        string s ; 
        cin >> s ; 
        int sum1  = 0 ;
        int sum2 = 0 ;
        for (int i = 0 ; i < 6 ; i++){
            if (i<3){
                sum1 +=s[i] ;
            }
            else {
                sum2 += s[i] ; 
            }
        }
            if (sum1==sum2){
                cout << "YES
" ; 
            }
            else {
                cout << "NO
" ; 
            }
 
    }
}